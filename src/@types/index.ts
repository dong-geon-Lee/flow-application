export interface CusProps {
  id?: number;
  callerName: string;
  phoneNumber: string;
  callNotes: string;
  status: boolean;
}

export interface EditProps {
  editName: string;
  editPhone: string;
  editNotes: string;
}
